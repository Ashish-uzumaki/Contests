#include<bits/stdc++.h>
using namespace std;
#define N 1e4

struct T{
    int arr[100000];
};

struct V{
    vector<int> v;
    V() : v(100000) {}
};


int main() {
    
    T t1;
    T t2;
    V v1;
    V v2;
    int n = N;
    
    // while(n--)
    // swap(t1, t2);

    while(n--)
    swap(v1, v2);
}